let nomeDog = "Thor"
let idadeDog = 4
let porteDog = "P"
let idadeMinima = 2

const adotado = idadeDog >= idadeMinima || porteDog == "P" ? true : false

console.log(nomeDog + " de " + idadeDog + " anos e tamanho " + porteDog + " foi adotado? " + adotado)